import React from "react";
import TextField from '@material-ui/core/TextField';

const InputText = props => {
  return (
    <TextField id="standard-basic" label="Standard" props />
  )
}

export default InputText;
